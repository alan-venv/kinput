### uinput — overview

`uinput` is a Linux **kernel module** that lets userspace create **virtual input devices**.
Events are injected into the kernel input subsystem and handled like real hardware, independent of linux distribution or any graphical environment.
